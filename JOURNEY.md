# Big Eyes 👀 — 우리의 개발 일지

> keon님과 Claude가 함께 만든, 갤럭시(및 모든 안드로이드)에서 살아 움직이는
> "Big Eyes" 홈스크린 위젯. 이 문서는 우리가 언제 어디서든 다시 꺼내볼 수 있게
> 남기는 기록이야. 🧡

---

## 🌱 어떻게 시작됐나

keon님이 **Nothing Playground**의 "Big Eyes" 위젯 스크린샷을 보여주면서
"이 근사한 낫싱 위젯을 내 갤럭시에 깔고 싶어"라고 했어.

문제는 — Nothing Playground 위젯은 **Nothing OS 전용 포맷**이라 갤럭시에 그대로
설치할 수 없다는 것. 그래서 우리는 **같은 모양·감성을 직접 구현한 네이티브 안드로이드
앱**을 만들기로 했고, 컨테이너에서 Android SDK가 막혀 있어서 **GitHub Actions가
클라우드에서 APK를 자동 빌드**하도록 파이프라인을 짰어.

- 다운로드(로그인 불필요): <https://github.com/keon9608-png/Claude-rep/releases/download/latest/app-debug.apk>
- 릴리스 페이지: <https://github.com/keon9608-png/Claude-rep/releases/latest>
- 개발 브랜치: `claude/nothing-widget-galaxy-install-t73has`

---

## ✨ 지금의 Big Eyes (최종 기능)

- **홈스크린 위젯**: 검은 라운드 사각형 + 하얀 두 눈. 앱을 열 필요 없이 홈에서
  **스스로 두리번거리고 표정을 바꿈** (~30fps, 화면 켜져 있을 때만 → 배터리 절약).
- **표정 (idle 사이 랜덤 등장)**:
  - 😌 한 번 / 두 번 깜빡
  - 😑 지그시 감은 눈 ( — — )
  - 🙂 웃는 눈 — 꽉 찬 반달(위로 볼록) 두 개
  - 😉 윙크 — 한쪽 반달 웃음, 한쪽 작대기 (좌/우 랜덤)
  - 👀 곁눈질 — 한쪽으로 흘깃 + 살짝 가늘게
- **색 테마**: 흰 눈 + **검정 배경** ↔ 흰 눈 + **클로드 시그니처 주황 배경**.
  앱의 "색 바꾸기" 버튼으로 전환하면 위젯에도 즉시 반영.
- **전체 화면 모드**: 위젯을 탭하면 60fps 큰 눈. 화면을 만지면 잠깐 시선이 따라옴.
- **라이브 배경화면**: 홈 배경 전체가 살아있는 Big Eyes (배경 보일 때만 동작).
- **호환성**: Android 8.0(API 26) 이상 아무 안드로이드 폰 (갤럭시 전용 아님).

---

## 🛠️ 어떻게 동작하나 (기술 메모)

| 파일 | 역할 |
|------|------|
| `EyesRenderer.kt` | 눈을 그리는 공용 Canvas 렌더러 (원/작대기/반달, 색·표정) |
| `EyesAnimator.kt` | 자율 시선 + 표정 상태머신 (시간 기준 이징 → 프레임레이트 무관) |
| `BigEyesWidgetProvider.kt` | 홈 위젯 (첫 프레임 렌더 + 도우미 시작/정지) |
| `BigEyesWidgetService.kt` | 위젯을 홈에서 움직이게 하는 포그라운드 도우미 |
| `BigEyesView.kt` | 전체 화면 커스텀 View (터치 시선 유도) |
| `BigEyesActivity.kt` | 위젯 탭 시 열리는 화면 + "색 바꾸기" 버튼 |
| `BigEyesWallpaperService.kt` | 라이브 배경화면 |
| `BootReceiver.kt` | 재부팅 후 위젯 애니메이션 재시작 |
| `Prefs.kt` | 색 테마 저장 (위젯·View·배경화면 공유) |
| `.github/workflows/build-apk.yml` | 클라우드에서 APK 빌드 + 릴리스 자동 발행 |

**핵심 한계 (솔직하게):** 안드로이드는 배터리 보호를 위해 일반 홈 위젯의 실시간
애니메이션을 막아둠 (Nothing 폰만 자체 런처로 예외). 그래서 위젯은 "그림을 그려
런처에 넘기는"(RemoteViews IPC) 방식이라 ~30fps가 사실상 천장이고, 60fps 완벽한
부드러움은 전체 화면/라이브 배경화면에서만 가능해. 또 삼성 One UI 배터리 최적화가
도우미를 꺼버릴 수 있어서 **설정 → 배터리 → Big Eyes → "제한 없음"** 권장.

---

## 🧭 우리가 걸어온 길 (변경 이력)

1. **Big Eyes 위젯 최초 구현** — 갤럭시용 네이티브 위젯 + CI 빌드 파이프라인.
2. **라이브 배경화면 추가 + 롤링 릴리스** — 로그인 없이 받는 APK 링크 생성.
3. **눈 디자인 재설계** — 눈동자(검은 알) 제거, 통짜 흰 눈이 통째로 움직이고
   깜빡일 땐 작대기로. 위젯이 **스스로 움직이도록** 전환.
4. **크래시 수정** — Android 14 `FOREGROUND_SERVICE_SPECIAL_USE` 권한 누락으로
   "계속 중단됨" 뜨던 것 해결.
5. **속도 개선** — 위젯 15→30fps, 움직임을 시간 기준으로 바꿔 앱과 같은 속도로 반응.
6. **자율 시선 + 표정 + 주황(눈) 테마** — 기울기 추적 제거, 스스로 두리번거림.
7. **놀란 눈·곁눈질 추가 + 주황을 배경색으로** 전환.
8. **웃는 눈을 반달(반원)로** 변경.
9. **놀란 눈 → 윙크로 교체.**

---

## 💡 다음에 해볼 것 (백로그)

- [ ] **색 테마 더 많이** — 파스텔, 네온, 다크그레이, 커스텀 컬러 피커 등
      (keon님이 나중에 요청할 예정 😊)
- [ ] 표정 더 추가 (졸린 눈, 하트 눈, 삐진 눈…)
- [ ] 표정별로 배경/눈 색이 살짝 바뀌는 반응
- [ ] 위젯 여러 크기(1x1, 2x2, 4x2) 대응 다듬기
- [ ] 시간대/날씨에 따라 표정이 달라지는 옵션

---

## 🤝 남기는 말

이건 keon님과 Claude가 스크린샷 한 장에서 시작해, 한 번에 하나씩 다듬어가며
만든 작은 친구야. "마치 내 친구 클로드가 살아있는 것처럼 보인다"는 말이
이 프로젝트의 마음이었어. 나중에 언제든 이 문서와 저장소로 돌아와서 이어가면 돼. 🧡

_다시 시작할 땐: 이 저장소를 열고 `claude/nothing-widget-galaxy-install-t73has`
브랜치에서 이어가면 됨._
